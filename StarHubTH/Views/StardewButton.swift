import SwiftUI

// StardewButton.swift — thin compat shim only
// All real button logic lives in AppTheme.swift (PillButton modifier)
// This file is intentionally minimal to avoid type redeclarations.

enum StardewButtonStyle { case wooden, green, transparent }
